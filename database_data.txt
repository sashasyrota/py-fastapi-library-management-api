PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE alembic_version (
	version_num VARCHAR(32) NOT NULL, 
	CONSTRAINT alembic_version_pkc PRIMARY KEY (version_num)
);
INSERT INTO alembic_version VALUES('b3f8b01bf00b');
CREATE TABLE author (
	id INTEGER NOT NULL, 
	name VARCHAR(64) NOT NULL, 
	bio VARCHAR, 
	PRIMARY KEY (id)
);
INSERT INTO author VALUES(1,'Taras Shevhenko','writer');
INSERT INTO author VALUES(2,'Ivan Franko','writer');
CREATE TABLE book (
	id INTEGER NOT NULL, 
	title VARCHAR(255) NOT NULL, 
	summary VARCHAR(511) NOT NULL, 
	publication_date DATE, 
	PRIMARY KEY (id)
);
INSERT INTO book VALUES(1,'Kobzar','Ukrainian book','1840-01-01');
INSERT INTO book VALUES(2,'Moysey','Ivan Franko''s book','1905-01-01');
CREATE TABLE association_table (
	author_id INTEGER NOT NULL, 
	book_id INTEGER NOT NULL, 
	PRIMARY KEY (author_id, book_id), 
	FOREIGN KEY(author_id) REFERENCES author (id), 
	FOREIGN KEY(book_id) REFERENCES book (id)
);
INSERT INTO association_table VALUES(1,1);
INSERT INTO association_table VALUES(2,2);
CREATE UNIQUE INDEX ix_author_id ON author (id);
CREATE INDEX ix_book_id ON book (id);
COMMIT;
