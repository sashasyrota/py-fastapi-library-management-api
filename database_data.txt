PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE alembic_version (
	version_num VARCHAR(32) NOT NULL, 
	CONSTRAINT alembic_version_pkc PRIMARY KEY (version_num)
);
INSERT INTO alembic_version VALUES('5db08167314b');
CREATE TABLE author (
	id INTEGER NOT NULL, 
	name VARCHAR(64) NOT NULL, 
	bio VARCHAR, 
	PRIMARY KEY (id), 
	UNIQUE (name)
);
INSERT INTO author VALUES(2,'Ivan Franko','writer');
INSERT INTO author VALUES(3,'Taras Shevhenko','Famous Writer');
INSERT INTO author VALUES(4,'Ivan Levytskiy','Famous Ukrainian writer');
CREATE TABLE book (
	id INTEGER NOT NULL, 
	title VARCHAR(255) NOT NULL, 
	summary VARCHAR(511) NOT NULL, 
	publication_date DATE, 
	author_id INTEGER, 
	PRIMARY KEY (id), 
	FOREIGN KEY(author_id) REFERENCES author (id)
);
INSERT INTO book VALUES(2,'Moysey','Ivan Franko''s book','1905-01-11',2);
INSERT INTO book VALUES(3,'Kobzar','Taras Shevchenko book','1840-01-11',3);
INSERT INTO book VALUES(4,'Mykola Djerya','Ivan Levytskyi second book','1885-01-11',4);
CREATE UNIQUE INDEX ix_author_id ON author (id);
CREATE INDEX ix_book_id ON book (id);
COMMIT;
